{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "useWorkspaces": true,
  "useNx": true,
  "version": "independent",
  "npmClient": "pnpm",
  "publishConfig": {
    "registry": "http://localhost:4873/"
  },
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": [
        "*.md",
        "tests"
      ]
    }
  }
}
